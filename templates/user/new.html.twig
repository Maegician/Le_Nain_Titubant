{% extends 'admin_base.html.twig' %}

{% block title %}{{'user.add'|trans}}{% endblock %}

{% block body %}
    <h1>{{'user.add'|trans}}</h1>

    {{ include('user/_form.html.twig') }}

    <a href="{{ path('user_index') }}">{{'list.return'|trans}}</a>
{% endblock %}
